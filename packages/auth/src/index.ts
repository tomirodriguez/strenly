/**
 * @strenly/auth - Better-Auth configuration
 * Exports factory functions for Cloudflare Workers compatibility
 */

export type { AuthEnv, AuthType, TAuth } from "./auth";
export { createAuth } from "./auth";
