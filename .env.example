# Database
# Local development uses postgres-js with direct connection
# Production uses @neondatabase/serverless with Neon cloud
DATABASE_URL=postgres://strenly:strenly@localhost:5432/strenly_dev

# Better Auth (REQUIRED)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here
# Must match the URL where your API is served
BETTER_AUTH_URL=http://localhost:8787

# Google OAuth (optional - leave empty to disable)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=